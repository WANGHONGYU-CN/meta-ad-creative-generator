# 复制本文件为 .env.local 并改成你的本地开发凭据。
# .env.local 已被 .gitignore 忽略（.env.* 规则），不会提交到仓库。
POSTGRES_USER=meta
POSTGRES_PASSWORD=change-me
POSTGRES_DB=meta_creative

# 另需在 WSL ~/.bashrc 配置（应用与 Alembic 都从环境变量读，缺失会明确报错）：
# export DATABASE_URL="postgresql+psycopg://meta:<密码>@localhost:5432/meta_creative"
